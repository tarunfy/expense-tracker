To Run The e2e Test
1. Open Terminal at project directory level
2. install the dependency by giving following command as (npm i)
3. give following another command as npx cypress Run
4. second way is in terminal type commond as npx cypress open -> it will open cypress execution termianl -> click on feature file -> it will start execution
